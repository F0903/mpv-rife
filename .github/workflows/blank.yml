name: Install and Upload Release

on:
  schedule:
    - cron: "0, 0, 1, *, *"
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install
      run: |
        "ls"
        "./install.ps1"
